schema_version: "0.1.0"
policy_id: missing-threshold
judge_id: harness-self-test-reviewer
rubric:
  - id: correctness
    description: Judge task correctness.
    scale: binary
calibration:
  labeled_sample_minimum: 5
  agreement_metric: percent_agreement
  stale_after_days: 90
  below_threshold_consequence: advisory-only
uncertainty_notes:
  - A judge policy without a numeric blocking threshold cannot produce blocking review.
